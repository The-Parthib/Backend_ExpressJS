<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign Up</title>
    <style>
      body {
        font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 20px;
        background-color: #000000;
        color: #ffffff;
      }
      form {
        max-width: 400px;
        margin: auto;
        background-color: #1a1a1a;
        padding: 20px;
        border-radius: 8px;
      }
      label {
        display: block;
        margin-top: 10px;
        color: #2bd144;
      }
      input {
        width: 100%;
        padding: 8px;
        margin-top: 5px;
        background-color: #333333;
        color: #ffffff;
        border: 1px solid #2bd144;
      }
      button {
        margin-top: 20px;
        padding: 10px;
        width: 100%;
        background-color: #2bd144;
        color: #000000;
        border: none;
        cursor: pointer;
      }
      button:hover {
        background-color: #1b8b2c;
      }
    </style>
  </head>
  <body>
    <h2 >MiniMizer || Login</h2>
    <form action="/auth/l/login" method="POST">

      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required />

      <label for="password">Password:</label>
      <input type="password" id="password" name="password" required />

      <button type="submit">Sign In</button>
    </form>
  </body>
</html>
